package com.jury1;

public class Helper {
    /**
     * Returns a greeting string addressed to the provided name.
     *
     * @param name The name to be included in the greeting message.
     * @return A greeting message that includes the provided name.
     */
    public static String greet(String name) {
        return "Hello, " + name + "!";
    }
}