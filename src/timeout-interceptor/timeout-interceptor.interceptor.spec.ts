import { TimeoutInterceptorInterceptor } from './timeout-interceptor.interceptor';

describe('TimeoutInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TimeoutInterceptorInterceptor()).toBeDefined();
  });
});
